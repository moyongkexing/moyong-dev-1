import type { VFC } from "react";
import { Layout } from "src/components/Layout";

const Contact: VFC = () => {
  return (
    <Layout>
      contact page
    </Layout>
  );
};
export default Contact;
